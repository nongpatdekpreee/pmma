version: '3'
services:
  app:
    build: .
    ports:
      - "3001:5000"
    depends_on:
      - db
    environment:
      DB_HOST: db

  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mydb  
